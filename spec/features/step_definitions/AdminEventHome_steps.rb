Given('AdminEventHome component is rendered with default properties') do
    pending
  end
  
  When('the ToggleButtonGroup changes with value {string} and mock response is successful') do |value|
    pending
  end
  
  When('the ToggleButtonGroup changes with value {string} and mock response fails with status {int}') do |value, status|
    pending
  end
  
  Then('the rendered component should match the snapshot') do
    pending
  end